spring:
#  datasource:
#    url: jdbc:h2:mem:test;
#    username: sa
#    password:
#      driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop // create : entity를 먼저 다 drop 하고 create하고 실행
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true
### spring boot에서는 별도의 설정을 안해주어도 test를 in-memory로 돌려버림.

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace
    org.hibernate.type: trace
